<div class="container">
 <div class="row">
  <div class="col-lg-0.5 col-lg-offset-0.5">

<h1>User info</h1>
<%= form_for(@user) do |f| %>

<% if @user.errors.any? %>
    <div id="error_explanation" class="alert alert-danger">
     <h2><%= pluralize(@user.errors.count, "error") %> prohibited this book from being saved:</h2>
      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<h4>Name</h4>
	<%= f.text_field :name, class:"name" %>
	<h4>Image</h4>
	<%= f.attachment_field :image, class:"user_image" %>
	<h4>Introduction</h4>
	<%= f.text_field :profile_introduction, class:"introduction" %>
	<br><%= f.submit 'Update User' %>

<% end %>

</div>
</div>
</div>

